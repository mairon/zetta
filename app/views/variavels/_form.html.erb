<%= form_for(@variavel) do |f| %>
  <% if @variavel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variavel.errors.count, "error") %> prohibited this variavel from being saved:</h2>

      <ul>
      <% @variavel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<ul class="tabs">
      <li><a href="#tab1">Variables - [Edicion]</a></li>
   </ul>

    <div class="tab_container">
      <div id="tab1" class="tab_content">
        <table>
          <tr>
            <td width="1000">
              <fieldset>
                <table>
                  <tr>
                    <td align="right">Nombre :</td>
                    <td><%= f.text_field :nome, :size => 80 %></td>                        

                    <td align="right">Sigla :</td>
                    <td><%= f.text_field :sigla, :size => 30 %></td>  
                  </tr>

                  <tr>
                    <td align="right">Valor :</td>
                    <td><%= f.text_field :valor, :size => 30 %></td>  
                  </tr>

                  <tr>
                    <td align="right">Unidad Métrica :</td>
                    <td colspan="4"><%= f.collection_select :unidade_metrica_id, UnidadeMetrica.all( :select => ' id,nome',
                                                            :order  => '2' ), 
                                                            :id,
                                                            :nome %></td>
                  </tr>

                  <%= f.hidden_field :unidade_metrica_nome %>

                </table>
              </fieldset>
            </td>
          </tr>
        </table>
      </div>
    </div>

    <div class="buttons">
      <%= f.submit :value => " Grabar ", :class => "button" %>
      <%= link_to 'Volver', variavels_path, :class => "button back" %>
    </div>
<% end %>
